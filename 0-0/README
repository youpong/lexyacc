yylex() が 0 を返す場合の振る舞いを確認するためのプロジェクト

トークン"end"が入力に含まれた場合、 ファイルディスクリプタ yyin は
閉じられ処理は終了する。

<BUILD>
$ make all
<TEST>
$ make check
<DEPENDS ON>
depends on: CUnit-2.1-3
Ubuntu-16.04
	# apt-get install libcunit1-dev
<UseCase1>
$ ./cl -f parse.y
use file parse.y
